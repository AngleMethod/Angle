# Angle email design

Platform notifications use lib/email.ts. Authentication HTML in auth/ is also installed in Supabase Authentication → Emails; changing these files alone does not update Supabase. Preserve each template’s variables and authentication link. Disabled security notifications remain disabled.
